{
  "name": "@agentefer/database",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "Acceso transaccional, tipos y repositorios de Supabase/PostgreSQL.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test",
    "test": "vitest run --root ../.. --config vitest.config.ts packages/database/test",
    "typecheck": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
